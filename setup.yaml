users:
  - name: feduser
    password: $FEDUSER_PASSWORD
    role: superuser
databases:
  - name: feddb
    owner: feduser
    tablespace:
      - name: tb1
        location: /appz/data/tb1
    schemas:
      - name: schema1
        authorised_user: feduser
        search_path: true
